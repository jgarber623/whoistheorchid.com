{
  "extends": "stylelint-config-standard",
  "plugins": [
    "stylelint-order"
  ],
  "rules": {
    "order/properties-alphabetical-order": true,
    "property-no-vendor-prefix": [
      true,
      { "ignoreProperties": ["interpolation-mode", "text-size-adjust"] }
    ]
  }
}
