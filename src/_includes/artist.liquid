<li class="h-card">
  {% if artist.url -%}
    <a href="{{ artist.url }}" class="p-name u-url">{{ artist.name }}</a>
  {% else -%}
    <span class="p-name">{{ artist.name }}</span>
  {% endif -%}
</li>
